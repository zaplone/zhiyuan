# 用户体验问题排查记录

**排查日期**: 2026-03-19

**问题描述**: 用户反馈产品详情页"卡顿"或"没有反应"，特别是移动端。

---

## 问题分析过程

### 第一步：初步判断错误

最初判断是 API 调用问题，认为详情页在运行时调用 API 获取数据导致慢。

### 第二步：确认网站架构

检查了 `next.config.js`，确认配置：
```javascript
output: 'export'  // 静态导出
trailingSlash: true
```

静态导出意味着：
- 构建时生成所有 HTML 文件（如 `out/en/products/sl-3001/index.html`）
- 用户访问时直接返回静态 HTML，不调用任何 API

### 第三步：找到真正原因

检查生成的静态资源：
```
out/_next/static/chunks/
  - fd9d1056-8bbbb75a48a3c1a6.js  (169KB)
  - framework-f66176bb897dc684.js (137KB)
  - main-0113fbfc665ede04.js      (114KB)
  - 117-4e6124c9deee650d.js       (122KB)
  - 总计约 500KB+ JS
```

**根本原因**：网站使用 Next.js 的 `<Link>` 组件和 `router.push()` 做客户端导航，但静态导出模式下需要加载大量 JS 才能工作。

用户每次点击链接时：
1. 浏览器需要下载/解析 ~500KB 的 JS
2. Next.js 客户端路由才能工作
3. 这个过程在移动端尤其慢

### 第四步：错误的解决方案

最初尝试用 `useTransition` + loading spinner 给用户反馈，但这只是让等待可见，不减少等待时间。

---

## 最终解决方案

**改用原生 `<a>` 标签跳转**，不使用 Next.js 客户端导航。

### 修改文件

1. **Header.tsx**
   - 移除 `useTransition`
   - 搜索功能：改用 `window.location.href`
   - 语言切换：改用 `window.location.href`

2. **ProductGrid.tsx**
   - 移除 `useTransition` 和 `useRouter`
   - 产品点击：改用 `window.location.href`

### 为什么 `<a>` 更正确

1. **静态网站的常规做法** - 传统静态网站都用 `<a>` 标签
2. **更快** - 不需要等待 JS 加载，直接加载 HTML
3. **更可靠** - 没有复杂的客户端路由逻辑

---

## 架构建议

如果未来要使用 Next.js 客户端导航特性，应该：
1. 放弃静态导出（`output: 'export'`）
2. 使用 Next.js 的动态服务器渲染或 ISR
3. 部署到支持 Node.js 的平台（如 Vercel）

但对于托管在 Cloudflare Pages 的静态网站，`<a>` 标签是最佳选择。

---

## 相关文件

- `/safetyshoe-frontend/src/components/Header.tsx`
- `/safetyshoe-frontend/src/components/ProductGrid.tsx`
- `/safetyshoe-frontend/next.config.js`